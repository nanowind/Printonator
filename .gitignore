# .NET
bin/
obj/
*.user
*.suo
.vs/
*.userprefs

# Test results
TestResults/
*.trx
# Test run logs (test_full.ps1 writes these — run output, không phải source)
tools/*.log
tools/*.log.err

# Build output
artifacts/
publish/

# AI workflow / local-only docs & state (never commit)
.agents-workflow/
six-phase-workflow/
.qoder/
.claude/
.kilo/
.codex/
.opencode/
.agents/skills
.agents-workflow.env
AGENTS.md
CLAUDE.md
SOUL.md
.mcp.json

# Local AI instruction memory (never commit)
.github/instructions/
# Impeccable design skill scaffolding (local tooling — CI workflow .github/workflows/ci.yml vẫn commit)
.github/agents/
.github/hooks/
.github/skills/
.impeccable/
skills-lock.json
# OS
Thumbs.db
Desktop.ini
.DS_Store

# IDE
.idea/
*.swp